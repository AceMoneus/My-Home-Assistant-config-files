  - sensor:
      - name: "Solvinkel"
        unique_id: solvinkel
        state: "{{ state_attr('sun.sun', 'elevation') | round(1) }}"
        attributes:
          azimuth: "{{ state_attr('sun.sun', 'azimuth') | round(1) }}"