
  update_ddns: 'bash /config/scripts/cloudflare_ddns.sh {{ states("sensor.external_ip") }}'
  git_commit_push: >
    cd /config && 
    git add . && 
    git config user.name "AceMoneus" && 
    git config user.email "kjell@moneus.se" && 
    git commit -m "{{ Automatic commit }}" && 
    git push
